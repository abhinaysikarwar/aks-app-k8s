apiVersion: v1
kind: Service

metadata:
  name: aks-app-backend-service
  namespace: aks-app

spec:
  selector:
    app: backend

  ports:
    - protocol: TCP
      port: 8001
      targetPort: 8001

  type: ClusterIP