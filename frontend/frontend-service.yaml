apiVersion: v1
kind: Service

metadata:
  name: aks-app-frontend-service
  namespace: aks-app

spec:
  selector:
    app: frontend

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80

  type: ClusterIP